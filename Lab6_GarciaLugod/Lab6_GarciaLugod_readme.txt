Program that runs two concurrent processes.
The parent process prints the local date and time every second with text for every third printed.
The child process executes the xclock function with a name of myXclock as the process.
Make sure to run ps -ax or ps -ef while the program is running to see the process name.