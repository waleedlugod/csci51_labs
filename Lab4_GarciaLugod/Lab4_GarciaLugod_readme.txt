This program accepts two integer input values, a multiplicand and multiplier and prints out their product.
If the multiplier is between 31 and -31 (inclusive), then the program uses bit shifting to get the result.
Otherwise, the program uses repetetive addition.